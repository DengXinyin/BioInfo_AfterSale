---
name: bioinfo-aftersales-workflow
description: 管理生信售后/返修项目：从需求澄清、原始数据追溯、可复现可视化、客户迭代到归档与经验沉淀。适用于扩增子（16S/ITS）、宏基因组、转录组、代谢组等项目的重绘、统计调整、数据提取、富集分析与结果交付。
---

# 必须遵守的四个原则

1. **编码前思考**：先识别错误假设、隐藏困惑和缺少的权衡，再开始编码。
2. **简洁优先**：优先采用简单、直接的实现，避免过度复杂和臃肿抽象。
3. **精准修改**：只修改完成任务所必需的内容，避免无关编辑和触碰不应修改的代码。
4. **目标驱动执行**：以测试优先和可验证的成功标准推动执行，完成后核验结果是否达标。

# 生信返修工作流

将每个售后任务做成可追溯、可复现、可复用的项目。案例需求和可复用代码见：

- `/home/xydeng/生信返修归档/售后SOP/客户售后需求汇总_20260717.md`
- `/home/xydeng/生信返修归档/售后SOP/代码库/代码资产索引.md`
- `/home/xydeng/生信返修归档/售后SOP/运行环境说明.md`

## 先确认的事项

开始前从工单、`Demand/` 文件和用户沟通中记录：项目编号、客户、分析类型、原始流程/数据路径、目标图或表、分组和比较、统计方法、需保留的原报告风格，以及交付格式。对下列高影响信息不猜测：

- 原始数据、原始作图代码和报告页码是否对应同一批流程；
- 使用 raw P value 还是 adjusted P value；
- 是否重算统计量，或只重绘既有结果；
- 客户的参考图是否同时约束颜色、字体、坐标、图例与布局；
- 宏基因组与扩增子项目的类型判定。

用户已经明确要求执行时直接开始；若关键信息缺失且会改变统计结论或交付内容，再提出最小必要的问题。

## 项目结构与命名

在 `/home/xydeng/` 创建项目，并只用下划线连接 AI 新建的名称：

```text
{项目编号}_{客户联系人}_{分析内容}/
├── README.md
├── Demand/     # 工单、示例图、原始脚本；保持原件不改
├── Input/      # 原始流程数据的项目内副本
├── Output/     # 正式交付 PDF、表格
└── Script/     # 可运行的 R/Python/Shell 脚本与最新运行日志
```

在 `README.md` 至少记录客户需求、分组/比较、输入来源、分析方法、输出清单、运行命令、修订记录，以及“客户需求提炼与反向追溯”。每个 `Input/` 文件须在对应脚本头部写明项目内路径、原始绝对路径和用途。

不要修改 `Demand/` 中的原件或原始流程数据。不要将临时 PNG、缓存、`__pycache__`、本地 R 包库或大体积中间文件作为正式交付物；客户明确要求时例外并写入 README。

## 数据追溯

优先从用户给出的绝对路径复制核心输入；若只给了 `Demand/script_*`，先从脚本追溯 Cromwell/Genostack workflow，再复制最小必要数据到 `Input/`。

常见扩增子路径：

| 目标 | 常见任务/文件 |
|---|---|
| 分类丰度 | `call-rename_table/.../tmp/tax_exp/{level}.tsv` |
| 分组、比较 | `call-restartNode/.../temp/*_sampleinfo.tsv`、`*_comparsion.tsv` |
| STAMP | `call-tax_stamp/.../stamp/{level}_group*.xlsx` |
| Alpha | `alpha_index.tsv` 与对应 `alpha*sampleinfo.tsv` |
| PCoA/NMDS | `call-tax_pcoa` / `call-tax_nmds` 下坐标表 |
| LEfSe | `call-tax_lefse/.../lefse/` 下 marker 表和原始作图参数 |

以流程模块而非项目名称判定类型：含 `assembly`、`megahit`、`binning`、基因预测/功能注释的通常为宏基因组；仅有 `tax_*`、`func_*` 与分类丰度表的通常为扩增子或功能预测。不确定时标“待确认”。

## 分析与可视化

1. 先读取工单和原报告，列出数据、统计和样式验收项。
2. 优先复用原始坐标、marker 表、差异结果；仅在客户要求或原结果不适用时重算。
3. 从 `售后SOP/代码库/代码资产索引.md` 选择最接近的案例脚本，复制到新项目 `Script/` 后参数化；不得直接在代码库的案例原件上开发。
4. 在脚本开头集中定义 `project_dir`、输入/输出路径、分组顺序、颜色、字体、统计开关和输出文件名。
5. 输出 PDF 到 `Output/`，用 `cairo_pdf` 或等效矢量设备；运行命令和标准错误均写入 `Script/{脚本名}_run_YYYYMMDD_HHMMSS.log`。
6. 交付前核对数据行数、样本分组、显著性来源、图例、坐标范围、字体和 PDF 可打开性。

默认视觉规范（客户或原报告优先）：Times New Roman，不可用时 Arial；主标题 20、轴标题 18、轴文字 16、图例标题 16、图例文字 14。正式交付默认 PDF；临时 PNG 仅用于检查并清理。

### 已验证的复用要点

- **STAMP**：直接读取 `all_res`；检查重复表头并强制数值列转换；明确 raw/adjusted P；高亮 taxa 可从 Excel 填充色提取；可一次输出 Times New Roman 与 Arial。
- **Alpha 多样性**：多组需求用 ANOVA + Tukey HSD；只标注显著两两比较；输出统计汇总表。
- **PCoA/NMDS**：先复用既有坐标；小样本下不要默认 `stat_ellipse(type="t")`。若原流程采用 `ggforce::geom_mark_ellipse()`，保持该算法；检查客户坐标范围内椭圆不被截断。
- **LEfSe**：从原 marker 表重绘 LDA；`lda_cutoff`、分类层级、分组配色作为参数。需要复刻 cladogram 时优先使用原 Docker 镜像/原脚本。
- **GO/KEGG/GSEA**：记录物种、ID 类型、背景基因集、上下调集合和校正方法；GO 默认 BP→CC→MF。离线环境优先使用本地 `TERM2GENE/TERM2NAME`；GSEA 非标准对象可用 base R 三面板重绘。
- **WGCNA**：先与原图核对 module eigengene 的正负号；不同 PCA/SVD 实现可能符号相反。默认标注相关系数与 P 值，模块色块、ME 前缀和颜色条均可配置。

## 售后 SOP 知识库：读取、复用与自动更新

将 `/home/xydeng/生信返修归档/售后SOP/` 作为售后经验的唯一知识库入口。每次开始新售后时，先按以下顺序读取：

1. 阅读最新的 `客户售后需求汇总_*.md`，用项目类型、图型和需求类别检索相似案例；
2. 阅读 `代码库/代码资产索引.md`，选择一个最接近的已验证案例；
3. 只读取所选资产及其原项目 README/Demand 中必要的输入格式和参数说明，避免把无关案例带入当前项目；
4. 需要确认运行环境或离线依赖时，阅读 `运行环境说明.md`；
5. 将“复用来源、改动参数、没有沿用的部分及原因”写入新项目 README。

结果通过后、归档前自动完成知识库补充：

1. 向最新需求汇总追加一行：项目编号、分析类型、客户核心需求、已验证做法、踩坑和报告端可前置改进项；若表结构不足，再增加小节，不覆盖既有记录；
2. 判断脚本是否具有跨项目复用价值。仅复制不含客户原始数据、密码或临时文件的交付脚本到 `代码库/{分析类型}/`；文件名使用“功能_原始案例_项目编号.扩展名”；
3. 在 `代码资产索引.md` 追加资产、必需输入、可配置参数、依赖和验证案例；明确硬编码路径/项目名必须替换；
4. 仅在 Conda、Docker、离线数据库或字体要求发生变化时更新 `运行环境说明.md`；
5. 同步在 `归档记录.md` 写入压缩包新路径、项目位置和归档日期。

没有产生新方法时，也要记录“复用现有案例、无新增资产”，避免为凑数量重复复制脚本。代码库中的案例原件不得直接修改；应复制到当前项目 `Script/` 后再开发。

## 运行与验收

使用统一环境：`conda activate bioinfo_aftersales`。额外包先检查环境与项目可复现性；不要把环境目录打入项目。耗时任务使用 `tmux`，例如：

```bash
Rscript Script/{脚本名}.R 2>&1 | tee Script/{脚本名}_run_YYYYMMDD_HHMMSS.log
```

每次客户反馈，先在 README 的修订记录写明“需求—修改—输出文件”，再调整。结果通过前逐项确认：输入来源、统计、所有比较组、排序/颜色、字体字号、坐标/截断、图例、输出命名与临时文件。

## 归档与沉淀

仅在用户明确确认结果并授权归档后，执行打包、移动或删除。操作前列出精确目标和命令，避免覆盖同名项目；归档文件和中转站目录重名时追加北京时间日期。

归档完成后（完整项目压缩包统一存放于 `/home/xydeng/生信返修归档/归档文件_压缩包/`）：

1. 追加 `/home/xydeng/生信返修归档/归档记录.md`，记录项目、类型、位置、日期和保留期；
2. 追加/更新 `售后SOP/客户售后需求汇总_YYYYMMDD.md` 的客户需求、关键参数、踩坑和前置改进项；
3. 若脚本可复用，保留一份不含客户数据的案例原件到 `售后SOP/代码库/{分析类型}/`，并更新代码索引；
4. 在索引中注明脚本所需输入格式、依赖、不可直接复用的硬编码路径和验证案例。

不要把工单、客户数据、API 密钥或内部绝对路径当作通用模板的一部分。案例脚本可保留原始路径注释以便追溯，但复用时必须替换。
