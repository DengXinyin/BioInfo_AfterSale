% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PCA_advanced.R
\name{plot_pca}
\alias{plot_pca}
\title{Draw a principal-component-analysis score plot}
\usage{
plot_pca(
  data,
  sample_column = "Sample",
  group_column = "Group",
  expr_columns = NULL,
  center = TRUE,
  scale = TRUE,
  x_pc = 1,
  y_pc = 2,
  group_colors = NULL,
  show_ellipse = TRUE,
  ellipse_level = 0.95,
  show_labels = FALSE,
  point_size = 3,
  point_alpha = 0.8,
  title = NULL,
  x_label = NULL,
  y_label = NULL,
  font_family = "sans",
  style = NULL,
  output_file = NULL,
  figure_width = 8,
  figure_height = 7,
  dpi = 300
)
}
\arguments{
\item{data}{Data frame containing the expression matrix plus, optionally,
sample and group columns.}

\item{sample_column}{Name of the sample identifier column.}

\item{group_column}{Name of the grouping column used to colour points.}

\item{expr_columns}{Optional character vector of expression columns to feed
into \code{\link[stats:prcomp]{stats::prcomp()}}. \code{NULL} uses every numeric column other
than the sample and group columns.}

\item{center, scale}{Logical centring and scaling passed to
\code{\link[stats:prcomp]{stats::prcomp()}}. Both default to \code{TRUE}.}

\item{x_pc, y_pc}{Principal components to plot on the x and y axes.}

\item{group_colors}{Named character vector mapping groups to colours. \code{NULL}
(default) uses the toolbox palette.}

\item{show_ellipse}{Whether to draw a per-group confidence ellipse.}

\item{ellipse_level}{Confidence level passed to \code{\link[ggplot2:stat_ellipse]{ggplot2::stat_ellipse()}}.}

\item{show_labels}{Whether to annotate each point with its sample label.}

\item{point_size}{Point size for samples.}

\item{point_alpha}{Point opacity from 0 to 1.}

\item{title}{Optional plot title.}

\item{x_label, y_label}{Axis labels. Defaults to \code{"PCn (xx.x\%\%)"}.}

\item{font_family}{Font family used by the shared plot style. \code{"sans"} is
the portable default.}

\item{style}{Optional object returned by \code{\link{choose_plot_style}()}. When
supplied, it replaces the font and text-size defaults.}

\item{output_file}{Optional PDF or PNG output path.}

\item{figure_width, figure_height}{Output dimensions in inches.}

\item{dpi}{PNG resolution.}
}
\value{
A ggplot object with the PCA scores (\code{.PC1}, \code{.PC2}, \code{.Group},
\code{.Sample}) stored in \code{plot$data}.
}
\description{
Performs PCA with \code{\link[stats:prcomp]{stats::prcomp()}} on a user-supplied expression matrix
and draws a two-component score plot with optional per-group 95% confidence
ellipses and optional sample labels. The colour scheme follows the shared
toolbox palette used across the 组学可视化百宝箱 modules by default.
}
