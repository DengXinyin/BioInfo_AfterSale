% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_survival.R
\name{plot_forest}
\alias{plot_forest}
\title{Draw a Cox regression forest plot}
\usage{
plot_forest(
  hr_table = NULL,
  model = NULL,
  data = NULL,
  formula = NULL,
  term_labels = NULL,
  title = "Cox Regression Forest Plot",
  point_color = "#E64B35",
  x_label = "Hazard Ratio (95% CI)",
  x_limits = c(0.1, 30),
  x_breaks = c(0.1, 0.5, 1, 2, 5, 10, 20),
  font_family = "sans",
  style = NULL,
  output_file = NULL,
  figure_width = 10,
  figure_height = 6,
  dpi = 300
)
}
\arguments{
\item{hr_table}{Optional data frame of hazard-ratio results with columns
\code{term}, \code{HR}, \code{lower}, \code{upper}, and optionally \code{p}. Provided when
\code{model} is \code{NULL}.}

\item{model}{Optional \code{\link[survival:coxph]{survival::coxph()}} model object. When supplied, the
table is computed from the model coefficients.}

\item{data}{Data frame used to fit \code{model} (required when \code{model} is given).}

\item{formula}{Cox model formula, e.g. \code{Surv(time, event) ~ group + age}.
Required when \code{model} is given.}

\item{term_labels}{Optional named character vector mapping model term names
to display labels.}

\item{title}{Optional plot title.}

\item{point_color}{Point colour for the hazard-ratio estimates.}

\item{x_label}{Axis label. Defaults to \code{"Hazard Ratio (95% CI)"}.}

\item{x_limits, x_breaks}{Axis limits and breaks (on the log10 scale).}

\item{font_family}{Font family used by the shared plot style. \code{"sans"} is the
portable default.}

\item{style}{Optional object returned by \code{\link{choose_plot_style}()}.}

\item{output_file}{Optional PDF or PNG output path.}

\item{figure_width, figure_height}{Output dimensions in inches.}

\item{dpi}{PNG resolution.}
}
\value{
A ggplot object.
}
\description{
Builds a forest plot of hazard ratios from a Cox proportional-hazards model
or its coefficient summary. When \code{model} is supplied, \code{\link[survival:coxph]{survival::coxph()}} is
assumed to have been run on \code{data} with \code{formula}; otherwise \code{hr_table} is
used directly and must contain \code{term}, \code{HR}, \code{lower}, and \code{upper} columns.
}
