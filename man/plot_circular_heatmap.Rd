% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Heatmap_circular.R
\name{plot_circular_heatmap}
\alias{plot_circular_heatmap}
\title{Plot a circular (ring) clustered heatmap}
\usage{
plot_circular_heatmap(
  matrix,
  scale = c("row", "none", "column"),
  cluster = TRUE,
  dend_side = c("inside", "outside"),
  rownames_side = NULL,
  show_rownames = TRUE,
  rownames_cex = 0.7,
  rownames_col = "black",
  show_colnames = TRUE,
  colnames_cex = 0.7,
  start_degree = 45,
  gap_degree = 45,
  track_height = 0.3,
  dend_track_height = 0.15,
  cell_border = "white",
  cell_lwd = 0.7,
  zscore_breaks = c(-2, 0, 2),
  heatmap_colors = c("#A5CC26", "white", "#FF7BAC"),
  na_color = "#BDBDBD",
  legend_title = "Exp",
  show_legend = TRUE,
  title = NULL,
  style = NULL,
  output_file = NULL,
  figure_width = 10,
  figure_height = 10,
  dpi = 300,
  draw_plot = is.null(output_file),
  ...
)
}
\arguments{
\item{title}{Optional heatmap title.}

\item{style}{A style object created by [choose_plot_style()].}

\item{matrix}{Numeric matrix or data frame of expression values. Rows are
genes (drawn along the circle), columns are samples (drawn as radial
layers).}

\item{scale}{Row/column scaling before plotting: `"row"` applies per-gene
Z-scores (default, recommended for expression matrices), `"column"`
applies per-sample Z-scores, and `"none"` plots values as supplied.}

\item{cluster}{Whether to cluster genes (rows). When `TRUE`, an average
linkage dendrogram on Euclidean distances is shown inside (or outside,
see `dend_side`) the ring.}

\item{dend_side}{Side for the gene dendrogram: `"inside"` or `"outside"`.}

\item{rownames_side}{Side for gene labels: `"inside"` or `"outside"`.
Defaults to the opposite of `dend_side`.}

\item{show_rownames}{Whether to draw gene (row) labels.}

\item{rownames_cex}{Gene-label size relative to the default device size.}

\item{rownames_col}{Gene-label colour.}

\item{show_colnames}{Whether to draw sample (column) labels as a radial
text layer inside the heatmap track.}

\item{colnames_cex}{Sample-label size relative to the default device size.}

\item{start_degree}{Degree position of the first sector (ring start).}

\item{gap_degree}{Angular gap between adjacent sectors.}

\item{track_height}{Relative height of the heatmap track.}

\item{dend_track_height}{Relative height of the dendrogram track.}

\item{cell_border}{Border colour of every heatmap cell.}

\item{cell_lwd}{Border line width of every heatmap cell.}

\item{zscore_breaks}{Three increasing numeric colour breakpoints used with
`heatmap_colors`.}

\item{heatmap_colors}{Three colours corresponding to `zscore_breaks`
(low / mid / high). When `scale = "none"` and these are not supplied, the
breaks are taken from the observed range of the matrix.}

\item{na_color}{Colour for missing values.}

\item{legend_title}{Title of the continuous colour legend.}

\item{show_legend}{Whether to draw the continuous colour legend.}

\item{output_file}{Optional PDF or PNG output filename. For a circular
heatmap, PDF is the recommended format (vector output).}

\item{figure_width, figure_height}{Output dimensions in inches.}

\item{dpi}{PNG resolution.}

\item{draw_plot}{Whether to draw to the current graphics device when
`output_file` is `NULL`.}

\item{...}{Additional arguments passed to [circlize::circos.heatmap()].}
}
\value{
Invisibly returns the scaled matrix that was plotted (rows in
  dendrogram order when `cluster = TRUE`).
}
\description{
Draws a gene-by-sample expression matrix as a circular heatmap with
per-row Z-score scaling (optional), hierarchical clustering of genes, an
inner (or outer) dendrogram, gene labels on the outer rim, sample labels
arranged radially, and a continuous colour legend. The implementation is
based on `circlize::circos.heatmap()`; the adjustable start angle and gap
angle follow the shared circular-heatmap tutorial workflow.
}
